[package]
name = "baogames"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.18"
noise = "0.9"
rand = "0.8"
serde = { version = "1", features = ["derive"] }
bincode = "1"

[profile.dev.package."*"]
opt-level = 1
